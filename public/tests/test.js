// Generated by CoffeeScript 1.9.1

  var assert;

  assert = chai.assert;


  suite("parsing", function() {
    test("Should parse NFA declarations", function() {
      var result;
      result = nfa.parse("NFA miNfa {(a)->(b);};");
      return assert.deepEqual(result.nodes.a.b, null);
    });
    test("Should parse NFA declarations with numeric links", function() {
      var result;
      result = nfa.parse("NFA miNfa {(a)-7->(b);};");
      return assert.deepEqual(result.nodes.a.b, "7");
    });
    test("Should parse NFA declarations with ID links", function() {
      var result;
      result = nfa.parse("NFA miNfa {(a)-c->(b);};");
      return assert.deepEqual(result.nodes.a.b, "c");
    });
    test("Should parse NFA declarations with two sided links", function() {
      var result;
      result = nfa.parse("NFA miNfa {(a)<-c->(b);};");
      assert.deepEqual(result.nodes.b.a, "c");
      return assert.deepEqual(result.nodes.a.b, "c");
    });
    test("Should parse NFA declarations with two sided epsylon links", function() {
      var result;
      result = nfa.parse("NFA miNfa {(a)<->(b);};");
      assert.deepEqual(result.nodes.b.a, null);
      return assert.deepEqual(result.nodes.a.b, null);
    });
    test("Should parse NFA declarations with final nodes", function() {
      var result;
      result = nfa.parse("NFA miNfa {(a)<-c->((b));};");
      assert.deepEqual(result.nodes.b.a, "c");
      assert.deepEqual(result.finals.b, true);
      assert.deepEqual(result.finals.a, undefined);
      return assert.deepEqual(result.nodes.a.b, "c");
    });
    test("Should parse NFA and test strings with epsylon transitions", function() {
      var result;
      result = nfa.parse("NFA miNfa {(a)<->(b); (b)-1->((c));};");
      return assert(result.testString("1"));
    });
    test("Should detect error", function() {
      var result;
      try {
        result = nfa.parse("NFA miNfa {(a)<-c->(b)};");
        assert(false);
      }

        catch(error) {

        assert(true);
      }


      //assert.deepEqual(result.nodes.b.a, "c");
      //return assert.deepEqual(result.nodes.a.b, "c");
    });
});
